# PRD — 產品需求文檔

> 這份文件是從既有程式碼、README、docs/ 反推整理而成(模式 E:舊專案改造)。
> 「為什麼做」「不做什麼」代碼看不出來,以下是 AI 根據現有文件與程式行為的推測,**需要您逐條確認或修正**。

## 1. 產品概述

- **一句話說清楚**:每個交易日自動抓取台股上市/上櫃的價格、籌碼、新聞、財報資料,用規則+量化混合策略算出「值得觀察」的候選股清單,產出 HTML/Markdown 報告發布到 GitHub Pages,並透過 LINE 推播摘要給使用者本人。
- **為什麼做**:(推測)取代人工每天盯盤篩股的重複勞動,把新聞催化、財務體質、估值、K 線型態、市場結構、籌碼流向等多個角度自動化整合成一份每日觀察清單,供使用者做進一步人工判斷。
- **目標用戶**:使用者本人(個人自用系統,非對外服務)。
- **明確定位**:輸出是「研究與觀察清單」,**不是**保證獲利的訊號,也**不會**自動下單。

## 2. 功能列表

### 必須有(MVP,目前已實作且每日運行中)
- [x] 每日排程自動抓資料(TWSE/TPEX 官方資料、FinMind、yfinance、twstock、RSS 新聞)
- [x] 混合策略評分(新聞催化 + 財務品質 + 估值風險 + 日線蠟燭圖 + 1H 市場結構 + 5M 流動性掃描/IFVG + 籌碼分析)
- [x] 台指(TAIEX)20MA 大盤狀態閘門,過濾純技術面突破訊號
- [x] 產業分散上限(避免同產業集中推薦)
- [x] 動態停損/停利追蹤(recommendation_tracker)
- [x] 報告產生(HTML dashboard + Markdown)並發布到 GitHub Pages
- [x] LINE 推播(report_link 摘要模式 / full_report 完整模式)
- [x] 每日只發一次的 marker 機制 + watchdog 補跑
- [x] CLI 手動觸發(validate-config / refresh-data / run / publish-pages / fetch-news 等子命令)
- [x] 量化研究平台(quant_research_platform):獨立回測、選股池管理、與 qlib/OpenBB/Kronos 對接的擴充分析能力

### 2026-07 新增功能(本輪開發)
- [x] **★最佳買點標籤**:在「發動確認(原新版策略)」既有入選條件之外,額外標記同時滿足「收盤價站上 60 日均線 + MACD 剛形成黃金交叉(DIF 於最近 2 個交易日內向上穿越訊號線)」的股票為 ★最佳買點。**只加標籤不加篩選**——不改變入選名單,但在報告排序與 LINE 推播中優先顯示
- [x] **大盤外資動向**:每日報告新增「外資動向」區塊,彙總 TWSE 三大法人(T86)資料,顯示外資近 10 個交易日對台股整體的每日買賣超(張)、連買/連賣天數,並給出偏多/偏空/中性的態度判讀
- [x] **策略漏斗式重命名**:三層策略改為角色導向命名並依使用順序排列——舊版策略→「品質底池(選股範圍)」、籌碼雷達→「主力動向(誰在買)」、新版策略→「發動確認(何時買)」,★最佳買點為頂層;報告與互動篩選介面依此重排。內部程式碼欄位名(`legacy_hit`/`chip_radar_hit`/`new_strategy_hit`)不變,僅改使用者可見文案

### 將來再說
- 使用者互動式篩選介面(目前 index.html 僅是靜態報告連結列表,無互動)
- 多使用者/多帳號支援
- 自動下單串接
- 個股外資持股比例變化追蹤(需新接 TWSE 外資持股比例 API)

## 3. 使用者流程(排程驅動,非互動式應用,以「一次每日執行」為單位)

1. GitHub Actions 於台北時間 17:30(週一至五)觸發 `daily_stock_analysis.yml`
2. refresh-data:抓取最新股價、籌碼、新聞等資料寫入 `data/`
3. refresh-quant-ohlcv / refresh-quant-realtime:量化平台資料更新
4. validate-config:檢查設定檔正確性,失敗則中止並提示
5. run:執行 hybrid 策略評分,產生報告寫入 `reports/`
6. publish-pages:發布報告到 GitHub Pages,更新 `index.html` 的報告連結列表
7. 檢查當日 LINE marker,若未發送則透過 LINE 推播摘要或完整報告
8. 17:40 / 17:50 watchdog 二次確認補跑,避免主流程失敗漏發

## 4. 頁面清單(本專案僅一個靜態展示頁,無互動路由)

| 頁面 | 主要內容和作用 |
|---|---|
| `index.html`(GitHub Pages 首頁) | 列出近期每日報告的連結,純展示,無表單、無登入 |
| 每日報告 HTML(`reports/*.html`) | 當日候選股清單、評分細節、圖表儀表板 |

## 5. 每個功能的完成定義

- **每日排程執行**:成功時當日 `reports/` 產出新的 HTML/MD 報告,GitHub Pages 更新,LINE 收到推播;失敗時 watchdog 應能偵測並補跑,或至少讓使用者從 GitHub Actions 執行紀錄看到失敗原因
- **混合策略評分**:輸出的候選清單需附評分依據(新聞/技術/籌碼/財務各分項可追溯),不是黑箱單一分數
- **大盤狀態閘門**:當 TAIEX 位處 20MA 之下等弱勢狀態時,應抑制純技術面突破訊號的推薦,降低追高風險
- **LINE 推播**:`report_link` 模式送出摘要+報告連結;`full_report` 模式送出完整 Markdown 內容(可能分段);當日已發送過則不重複發送
- **CLI 指令**:每個子命令需有清楚的成功/失敗結束碼與訊息,方便排程與人工除錯

## 6. 數據字段(核心資料實體,細節見 ARCH.md 第 7 節)

- **股票基本資料**:代號、名稱、市場別(上市/上櫃)、產業別
- **價格資料**:日線 / 1H / 5M 三層,欄位 `symbol,datetime,open,high,low,close,volume`
- **籌碼快照**:法人/主力買賣超等(chip_snapshot)
- **新聞資料**:RSS 來源,含催化事件分類
- **推薦紀錄**:candidate、評分、進場/停損/停利價位、追蹤狀態(recommendation_tracker)

## 7. 錯誤狀態

- **外部資料源掛了**(TWSE/TPEX/FinMind/yfinance 逾時或格式變動):應有 fallback 或快取降級,並在報告/日誌中標示資料不完整,不應讓整條流程直接中斷發不出報告
- **LINE API 失敗**:應記錄失敗但不讓報告發布流程整體失敗;watchdog 負責補發
- **設定檔錯誤**:`validate-config` 應在流程最前面攔截,給出明確錯誤欄位,不進入後續昂貴的資料抓取與運算步驟
- **無資料/空清單**:報告應明確顯示「今日無符合條件標的」,而非留白或報錯

## 8. 不做什麼 ⚠️

- 不做自動下單或任何資金移轉
- 不提供個人化投資建議背書(僅是研究觀察清單,使用者需自行判斷)
- 不做多使用者系統、不做登入權限體系(自用工具)
- 不承諾即時(秒級)行情,以排程批次為主,realtime proxy 僅輔助當日評分用途
- 不在前端(index.html/報告 HTML)嵌入任何 API Key 或憑證

## 8.5 本輪新功能完成定義與驗收

- **★最佳買點**(2026-07-19 微調):
  - 定義:收盤價在最近 2 個交易日內**剛向上突破** 60 日均線(前一日收盤在 60MA 之下、當日站上,且最新收盤仍在 60MA 之上),且 MACD(12,26,9)的 DIF 在最近 2 個交易日內由下往上穿越訊號線(剛金叉,非早已金叉)
  - 早已站上 60MA 多日者不標記(不是「剛突破」)
  - 資料不足時一律不標記
  - 驗收:報告中被標記的股票顯示 ★;排序上 ★ 股票優先於未標記者;既有候選名單數量不因此變少
- **☆短線買點**(2026-07-19 新增;2026-07-19 稍後調整為第一優先):
  - 定義:同 ★ 的邏輯但均線改為 20 日均線——收盤最近 2 個交易日內剛向上突破 20MA 且仍在其上,且 MACD 剛金叉
  - **優先序:☆短線買點為第一優先,★最佳買點為第二優先**;同時滿足兩者時顯示雙標記「☆★」且排第一優先
  - 驗收:報告排序 ☆ > ★ > 一般分層;雙命中股票顯示 ☆★
- **股價分類**(2026-07-19 新增;2026-07-20 補篩選控制項):
  - 定義:依最新收盤價分為 低價位(30 元以下)/ 中價位(30~80 元)/ 高價位(80 元以上);30 元整歸中價位、80 元整歸中價位
  - 配套變更:移除品質底池選股的 50 元價格上限(保留 10 元下限與日均成交金額 5 千萬門檻),讓中高價股可進入候選池,否則高價位分類永遠為空
  - **篩選控制項**:互動報告左側新增「股價分類」三個勾選框(預設全開),與漏斗三層以 AND 合併過濾股票下拉選單與圖表;三個都不勾時視同無符合股票。綜合關注榜維持顯示全部排名(不受價位篩選影響),但每列標示價位
  - 驗收:報告候選表格與個股摘要顯示價位分類;勾選單一價位時下拉選單只剩該價位股票;80 元以上股票在流動性達標時可出現在候選清單

- **K 線圖縮放/平移**(2026-07-20 新增):
  - 桌機:滑鼠滾輪以游標位置為錨點縮放、按住拖曳左右平移、雙擊還原
  - 手機/平板:雙指 pinch 縮放、單指水平拖曳平移(垂直滑動仍可捲動頁面)
  - 另提供「重設縮放」按鈕;切換股票或變更篩選時自動還原全區間
  - 縮放時 Y 軸價格區間依可見範圍重算(不是單純放大圖片),成交量/MACD/RSI 副圖同步
  - 驗收:滾輪縮放後仍能點擊定位單一 K 棒且資訊面板正確;最少可縮放至約 10 根 K 棒;手機雙指縮放不會誤觸發頁面縮放
- **外資動向區塊**:
  - 驗收:報告含近 10 個交易日的外資每日買賣超(張)列表、目前連買/連賣天數、態度判讀(偏多/偏空/中性);T86 資料抓不到時區塊顯示「外資資料暫缺」而非整份報告失敗
- **漏斗式重命名**:
  - 驗收:報告與互動篩選介面依「品質底池 → 主力動向 → 發動確認 → ★最佳買點」順序呈現,每層附一句話角色說明;`pytest` 全數通過(相關測試文案同步更新)

## 8.6 璞玉方法論整合(2026-07-21 起,分階段)

> 來源:使用者提供的六階段「臺灣璞玉指數」選股/操作方法論。經比對後決定**以現有 hybrid 選股為骨幹,把方法論當增強層逐切片接入,不推倒重來**。三個已定調的設計決策:
> 1. 整合方式=分階段分切片逐步做
> 2. ★/☆(剛突破,右側)與「超跌抄底」(跌破季線+低檔背離,左側)**兩種都保留、分開標記**
> 3. 第六階段 333 資金管理=做成**通用規則說明**(衛教式),不做個人化持倉下單指令(守住「不提供個人化投資建議」邊界)

### 切片 P2 —— 部位管理原則(333 翻倍計畫,通用教育說明)
- **定義**:報告新增靜態文字區塊,說明「30% 分趟出場 + 拉回再進場」的通用資金管理觀念與 1.3³≈2.2 倍複利算法
- **邊界**:純教育性質,**不追蹤任何人實際持股、不下單、不提供個人化投資建議**;內容不得出現具體個股代號或針對特定部位的指令
- **顯示位置**:報告「推薦追蹤(勝率閉環)」區塊之後
- **驗收**:區塊固定出現、文案含明確的免責聲明字句(不追蹤個人部位/不下單/不提供個人化投資建議);不含任何股票代號

### 切片 P1 —— ◆超跌抄底(左側逆向訊號)
- **定義**:純用既有日線 OHLCV,不需新資料源。同時滿足:
  1. 收盤價 < 60 日均線(季線)——需 ≥ 61 根日線,否則不標記
  2. 收盤價 = 近 `DIP_LOOKBACK`(20)日最低收盤(創波段新低)
  3. KD 的 K 值 < 40(低檔)**且** K 值 > 同區間 K 的最低值(價創新低但 K 沒同步破底 = 低檔背離)
- **與 ★/☆ 的關係**:★/☆ 是右側「剛突破」、◆ 是左側「超跌搶反彈」,三者互相獨立、各自標記;一檔可同時帶多個標記
- **顯示**:新標籤「◆超跌抄底」;圖表 payload 帶 `dipReversal` 旗標;LINE 摘要前綴 ◆;綜合關注榜排序置於 ☆★ 之後、漏斗組合之前(左側訊號風險高於右側已發動,但屬方法論主打的安全墊買點)
- **驗收**:跌破季線+創新低+K背離的股票顯示 ◆;早已站上季線或 K 同步破底者不標記;資料不足不標記;`pytest` 全過

### 切片 P7 —— 璞玉指數 IX0231 每日收盤 + 與大盤相對強弱
- **資料源**:`backend.taiwanindex.com.tw/api/indexes/IX0231/records`(見下方查證結果),快取 4 小時,失敗時降級不中斷報告
- **定義**:報告新增「璞玉指數動向」區塊,顯示最近 `PRISTINE_LOOKBACK_SESSIONS`(5)個交易日的璞玉指數漲跌幅、同期間 TAIEX 漲跌幅、相對強弱判讀:
  - TAIEX 跌幅 ≥1% 且璞玉相對強度(璞玉漲跌幅－TAIEX漲跌幅)≥1% → 「璞玉抗跌(資金避風港)」
  - 相對強度 ≤ -1% → 「璞玉走弱」
  - 其餘 → 「同步」
- **邊界**:資料不足(<6 個交易日)或抓取失敗時顯示「璞玉指數資料暫缺」,不中斷報告
- **驗收**:報告含璞玉/TAIEX 近5日漲跌幅數字與判讀文字;`pytest` 全過;已用真實資料端到端驗證

### 切片 P3 —— 融資餘額動向(大盤市場層級)
- **資料源**:`www.twse.com.tw/rwd/zh/marginTrading/MI_MARGN?response=json&date=YYYYMMDD`(見下方查證結果),支援歷史日期回溯查詢,快取 30 分鐘,失敗降級不中斷報告
- **定義**:報告新增「融資餘額動向」區塊,取近 5 個交易日的融資金額(仟元換算億元)每日增減,判讀:
  - 連續 ≥3 天每日減少且累計減少 ≥300 億元 → 「融資急縮(籌碼清洗,留意落底訊號)」
  - 最新一日單日增加 ≥50 億元 → 「融資急增(散戶追價,留意主力調節風險)」
  - 其餘 → 「持平」
- **範疇說明**:這是方法論原文描述的**大盤市場層級**指標(每日 18:30 證交所公布後看整體散戶浮額),不是個股評分因子。專案裡另有 `data/capital_flow/margin_change.py` 這個**個股層級**的融資分析模組,本次**刻意不接線**——它需要逐股的融資融券資料(FinMind `TaiwanStockMarginPurchaseShortSale`,個股逐檔查詢有速率限制,對每日候選清單掃描不划算),與 P3 的大盤判讀是兩件事,列為未來可選項目(非本次必要)
- **驗收**:報告含近5日融資餘額與增減表格、判讀文字;假日/非交易日正確跳過;`pytest` 全過;已用真實資料端到端驗證

### 切片 P6 —— 外資期貨未平倉 + 官股券商分點買超
- **外資期貨未平倉(大盤層級)**:
  - 資料源:期交所 OpenAPI `MarketDataOfMajorInstitutionalTradersDetailsOfFuturesContractsBytheDate`(只回最新一日,無歷史回溯參數)
  - **刻意不做多空方向判讀**:方法論原文明確警告「外資期貨空單在高檔(8萬~9萬口)不代表崩盤,常是搭配槓桿型ETF的無風險套利避險」,故此區塊只顯示原始多空口數 + 固定的提醒文字,不產生「看多/看空」結論,避免落入原文警告的誤判陷阱
  - 驗收:報告顯示最新一日外資臺股期貨多單/空單/淨部位口數與提醒文字;資料不可得時降級不中斷報告
- **官股券商分點買超(個股層級)**:
  - 資料源:重用既有已接線的 `broker_source.py`(HiStock 分點,已在生產環境提供 `top10_main_force_brokers`)+ 新增靜態官股銀行關鍵字表(兆豐/合庫/第一金/華南/彰銀/臺企銀/土地銀行/臺灣銀行等),**不需要新的網路請求**——資料本來就已經抓回來,只是原本被丟棄在合併字串裡未拆解
  - `chip_snapshot.py` 的 `BrokerChipSummary`/`build_tw_chip_snapshot_csv` 新增 `official_broker_net_buy` 欄位;`HybridRow` 新增對應欄位,顯示於候選股票分析表「官股買超」欄與圖表籌碼快照面板
  - **範疇說明**:方法論原文強調官股買超「標的一定是大型權值股」,本次先如實呈現數字(哪些股票有官股買超),尚未加上市值/權值股門檛過濾,留待未來視需要疊加
- **驗收**:兩個子功能各自新增測試(純函式判讀 + 資料流轉);`pytest` 全過;已用真實資料端到端驗證(實測外資淨空單 85,380 口,落在方法論原文舉例的 8~9 萬口範圍內)

### 切片 P5(含 P4)—— 璞玉健康指標(EPS/ROE/股利)+ 選股邏輯重跑當母池
- **資料源**:FinMind `TaiwanStockFinancialStatements`(EPS、稅後淨利)、`TaiwanStockBalanceSheet`(股東權益、負債、總資產)、`TaiwanStockDividend`(股利發放),`finmind.py` client 新增對應方法
- **硬篩選(對應 P4 母池)**:`evaluate_pristine_screen` 純函式,依 TIP 官方指數概覽 PDF 揭露的規則——最近4季EPS皆為正、最近3年EPS皆為正、最近3年皆有配息,三者皆通過才算「璞玉候選」。已用真實 FinMind 資料端到端驗證(台積電 2330 通過篩選,ROE 34.5%、負債比 30.9%,數字皆與已知財務常識相符)
- **誠實範疇聲明**:方法論原文的完整加權公式(30%估值 + 30%成長性 + **20%產業地位/實質AI含金量** + 10%ROE現金流 + **10%市場非擁擠度百分位**)有兩項(粗體)需要主觀產業標記與全市場排名百分位,本專案沒有這些資料,**刻意不捏造假精確的加權總分**,只提供可誠實算出的子項:EPS/股利硬篩選(通過/不通過)、ROE%、負債比%、PE 估值判讀(`describe_valuation`:甜蜜區間10~15倍/低於20倍門檻/超過上限)
- **未接線進每日報告**:FinMind 個股查詢有速率限制(免 token 情況下更嚴格),對每日候選清單逐股掃描不符成本效益,且會拖慢/可能卡住每日排程。本次只完成核心邏輯與資料擷取層(可獨立呼叫、已用真實資料驗證),**尚未接 CLI 指令或排程**,留待未來視需求(例如每週跑一次而非每日)另立切片
- **驗收**:18 個純函式測試涵蓋篩選通過/四種不通過情境/ROE/負債比/PE估值判讀/年度加總/股利年度彙整;`pytest` 全過;已用真實資料端到端驗證解析邏輯正確

### 資料源查證結果(2026-08-26)

逐項查證後,四項有可直接接的真實免費 API,一項(P4 完整成分股清單)確認官方不對外公開,改用等價替代方案:

| 切片 | 資料源查證結論 |
|---|---|
| P7 璞玉指數 | ✅ 真實公開 JSON API(從 TIP 官網前端 JS 反解出,非官方文件端點,無需驗證):`GET https://backend.taiwanindex.com.tw/api/indexes/IX0231/records?start=YYYY-MM-DD&end=YYYY-MM-DD`,已實測成功。回傳 price(指數點位)/return(含息報酬)/波動點數/波動百分比 |
| P3 融資餘額 | ✅ 真實公開 JSON API(大盤/市場總量):`GET https://www.twse.com.tw/rwd/zh/marginTrading/MI_MARGN?response=json`,已實測成功。回傳融資/融券/融資金額的買進/賣出/現金(券)償還/前日餘額/今日餘額 |
| P6 外資期貨未平倉 | ✅ 官方 OpenAPI 有此端點:`openapi.taifex.com.tw` 的 `MarketDataOfMajorInstitutionalTradersDetailsOfFuturesContractsBytheDate`(三大法人-區分各期貨契約-依日期),含外資期貨多空未平倉口數;每次僅回最新一日,需比照現有 T86 模式逐日累積存快取。官股券商分點=資料整理(用既有 `broker_source.py` histock 分點資料 + 靜態官股銀行名稱表比對),非 API 問題 |
| P5 EPS/ROE/股利 | ✅ FinMind `TaiwanStockFinancialStatements`(已實測含 EPS 欄位,免 token 基本額度可用)、`TaiwanStockBalanceSheet`(股東權益,算 ROE)、`TaiwanStockDividend` |
| P4 璞玉331檔成分股清單 | ❌ **確認官方不公開完整清單**。深入 TIP 官網下載檔案(指數概覽 PDF)確認只公開「權重前十大成分股」,完整清單是商業授權資料。**替代方案**:PDF 揭露的官方選股邏輯(3年+4季EPS為正、3年皆配息、排除成交量/週轉率/漲幅/波動度前10%過熱股)與 P5 健康評分要抓的資料完全相同,故 **P4 併入 P5**——用 FinMind 財報資料重新跑一次官方公開的篩選邏輯,產出精神上等價的母池,不強求取得原始清單 |

### 後續切片(依查證結果排序,待逐一實作)
- **P7** 璞玉指數 IX0231 每日收盤 + 與大盤相對強弱
- **P3** 融資餘額判讀(接既有孤島模組 `data/capital_flow/margin_change.py`)
- **P6** 大盤情緒:外資期貨空單 + 官股券商分點買超
- **P5**(含 P4)璞玉健康評分:EPS/ROE/股利 3 年連續 + 自行重跑選股邏輯當母池
- **P8** 產業鏈上中下游群體共識判讀(需產業鏈對應表,尚未查證)

## 9. 驗收清單

- [ ] `python -m stock_signal_system.cli validate-config --config configs/local.example.json` 可正常通過
- [ ] `python -m stock_signal_system.cli run --config configs/local.example.json` 可產生報告於 `reports/`
- [ ] GitHub Actions `daily_stock_analysis.yml` 手動觸發(workflow_dispatch)可完整跑完不報錯
- [ ] LINE 推播在測試 config 下可正確送達(或至少不拋例外)
- [ ] `pytest` 全數通過
- [ ] GitHub Pages 上 `index.html` 能看到最新報告連結
